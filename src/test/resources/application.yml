spring:
  jpa:
    show-sql: true
  h2:
    console:
      enabled: true
  session:
    store-type: jdbc

  security:
    oauth2:
      client:
        registration:
          google.client-id: test
          google.client-secret: test
          google.scope:
            - profile
            - email

# Test OAuth